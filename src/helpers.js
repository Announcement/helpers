import * as $is from './is'
import * as $as from './as'
import * as $query from './query'

exports.are = $is
exports.as = $as
exports.is = $is
exports.query = $query

// clone,
// combine,
// has,
// inject,
// negated,
// prepare
