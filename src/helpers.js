/** module helpers */
import * as is from './is'
import * as as from './as'

// let helpers = {is, as}
const are = is
export {is, are, as}
  // clone,
  // combine,
  // has,
  // inject,
  // negated,
  // prepare
