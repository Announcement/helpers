'use strict'

/**
 * Clones an object.
 *
 * @function clone
 *
 * @param {Object} object - The object to be cloned.
 *
 * @returns {Object} The clone of the object.
 */

var clone = function (object) {
  return Object.assign({}, object)
}

module.exports = clone

//# sourceMappingURL=map/clone.js.map
